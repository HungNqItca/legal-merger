Check required dependencies for Legal Merger.

Run:

```
pip show python-docx pdfplumber openpyxl PyQt6
```

Report which packages are installed (with their versions) and which are missing. If any required package is missing, show the install command: `pip install <package-name>`. 
