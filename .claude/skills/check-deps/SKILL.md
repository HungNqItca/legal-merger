
---
description: Kiểm tra việc cài đặt thư viện cho Legal Merger.

---

Run:

```
pip show python-docx pdfplumber openpyxl PyQt6 langchain
```

Liệt kê tất cả các package đã được cài đặt (bao gồm cả phiên bản) và package nào chưa được cài đặt. Với mỗi package còn thiếu, hiển thị hướng dẫn cài đặt: `pip install <package-name>`. 
