# Legal Merger — dependencies
# Cài đặt: pip install -r requirements.txt
# Cài đặt kèm GUI: pip install -r requirements.txt PyQt6

python-docx>=1.2.0
pdfplumber>=0.11.9
openpyxl>=3.1.5

# Chỉ cần khi dùng giao diện đồ họa (python -m legal_merger --gui)
# Bỏ comment dòng dưới hoặc chạy: pip install PyQt6
PyQt6>=6.11.0
